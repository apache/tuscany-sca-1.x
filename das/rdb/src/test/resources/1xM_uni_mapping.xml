<?xml version="1.0" encoding="ASCII"?>
<Config>
  <Relationship name="orders" primaryKeyTable="CUSTOMER" foreignKeyTable="ANORDER" many="true">
      <KeyPair>
        <primaryKeyColumn>ID</primaryKeyColumn>
        <foreignKeyColumn>CUSTOMER_ID</foreignKeyColumn>
      </KeyPair>
    <update>update anorder set CUSTOMER_ID = :CUSTOMER_ID where ID = :ID</update>
  </Relationship>
  <Table name="CUSTOMER">
  	<Column name="ID" primaryKey="true"/>
    <create>insert into customer values (:ID, :LASTNAME, :ADDRESS)</create>
    <update>update customer set lastname = :LASTNAME where ID = :ID</update>
    <delete>delete from customer where ID = :ID</delete>
  </Table>
  
  
  <Table name="ANORDER">
 	<Column name="ID" primaryKey="true"/>
  	<update>update ANORDER set product = :PRODUCT, customer_id = :CUSTOMER_ID, quantity = QUANTITY where ID = :ID</update>
  </Table>
</config>

